version: '2'
services:
  dmgl-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/DMGL/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=DMGL
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
